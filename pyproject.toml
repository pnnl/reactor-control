[project]
name = "reactor-control"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.0",
    "pyserial>=3.5",
    "pymodbus>=3.0.0",
    "pyyaml>=6.0.0",
]
