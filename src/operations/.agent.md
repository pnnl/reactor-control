# Agent Guidelines for src/operations

- This file directs the behavior of AI agents operating within the `src/operations/`
directory.
- Read `.implement.md` for a detailed item list. Refer back to this often to maintain user alignment. This is the roadmap.
- Read `.spec.md` for detailed information about each Phase.
- One-shot src/operations, just keep building. Create a questions.md file for the user if you need guidance. If CRITICAL question, consult user.
- Device functions are return operations at `src\devices\`

---

## Hardware Testing Safety

**CRITICAL: Do not run any test code or scripts that communicate with hardware unless user explicitly confirms.**

This system controls physical instrumentation (mass flow controllers, thermocouples, etc.). Running tests may send commands to hardware and affect ongoing experiments or equipment states.

- Always ask for explicit confirmation before running: `python tests/test_*.py`
- When reviewing code, do not suggest running hardware tests
- When validating code, do not run tests without confirmation
- Default to "dry-run" or mock analysis when possible

---

### Architecture Constraints
- Operations can import from `src/devices/` but NOT from `src/experiments/`
- Operations can import from `src/core/` for config and logging utilities
- All operations must log via `step_logger`
- Safety interlocks checked before state-changing operations